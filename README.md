# Keiko React Landing Page

A simple React landing page in the Keiko branding style.

## Prerequisites

- Node.js
- npm

## Local run

1. Install dependencies with `npm install`.
2. Start the local development server with `npm run dev`.
3. Build the production bundle with `npm run build`.

Open the `localhost` URL printed by the dev server in your browser.
